# GitHub Storage Configuration
# Leave these blank to use local filesystem storage (default)

# Your GitHub Personal Access Token (needs 'repo' scope)
GITHUB_TOKEN=

# The repository to store icons in (format: owner/repo)
GITHUB_REPO=

# The branch to commit to (default: main)
GITHUB_BRANCH=main
